check host node with address 127.0.0.1
    start "/sbin/start node"
    stop "/sbin/stop node"
    if failed port <%= node['app']['port'] %> protocol HTTP
        request /
        with timeout 5 seconds
        then restart